—уществует условный контейнер дл€ записей (строк), который хранит их в сортированном пор€дке. «аписи посто€нно добавл€ютс€ и удал€ютс€. ѕри этом из контейнера с той же периодичностью считываютс€ данные по пор€дковому номеру.

Ќеобходимо реализовать контейнер (без использовани€ многопоточности), который будет иметь следующее api:
- вставить новый элемент,
- удалить элемент по пор€дковому номеру,
- получить элемент по пор€дковому номеру.

ƒл€ проверки алгоритма необходимо использовать приложенные тестовые данные и заготовку теста.

‘айл test.cpp - заготовка теста.
‘айл write.txt - последовательность строк дл€ первоначальной вставки.
‘айл modify.txt - последовательность индексов дл€ удалени€ и строк дл€ вставки разделенные пробелом.
‘айл read.txt - последовательность индексов дл€ чтени€ и строк дл€ проверки разделенные пробелом.

јлгоритм теста следующий - в заполненном контейнере данными из write.txt происходит последовательное обновление (удаление по индексу и вставка новой строки) записей из modify.txt и чтение (по заданному индексу с последующей проверкой) из read.txt.

 онтейнер должен обработать весь набор данных за врем€ не превышающее 5 секунд (врем€ актуально дл€ процессора с тактовой частотой 2.6√√ц).

ƒл€ реализации можно использовать любые инструменты стандартной библиотеки с++.

ƒл€ проведени€ отладки есть набор с меньшим количеством данных
